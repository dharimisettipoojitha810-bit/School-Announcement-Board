services:
  - type: static_site
    name: school-announcement-board-frontend
    env: static
    region: oregon
    plan: free
    buildCommand: npm install && npm run build:frontend
    publishPath: frontend/dist
    envVars:
      - key: VITE_API_URL
        value: https://<your-vercel-backend-url>
